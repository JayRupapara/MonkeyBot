import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';  // Import using curly braces
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
